<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= power.name %>
      <br>
      <%= link_to power.description ,power_path(power.id) %>
    </li>
    <br>
  <% end %>
</ul>
